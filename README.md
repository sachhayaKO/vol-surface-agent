# vol-surface-agent

Options volatility analysis agent. Built in phases:
1. Raw tool-calling (no framework)
2. LangChain + create_agent
3. Custom LangGraph state machine
4. Evals + LangSmith observability

